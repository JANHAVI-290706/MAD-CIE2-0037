import React, { useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
} from 'react-native';

const CreateAccountScreen = () => {
  const [password, setPassword] = useState('');
  const [checked, setChecked] = useState(false);

  return (
    <View style={styles.container}>
      
      {/* Header */}
      <Text style={styles.header}>Create Account</Text>

      {/* Social Buttons */}
      <TouchableOpacity style={styles.socialBtn}>
        <Text>GitHub</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.socialBtn}>
        <Text>Google</Text>
      </TouchableOpacity>

      {/* Separator */}
      <Text style={styles.separator}>
        Or sign up with credentials
      </Text>

      {/* Inputs */}
      <TextInput placeholder="Name" style={styles.input} />
      <TextInput placeholder="Email" style={styles.input} />

      <TextInput
        placeholder="Password"
        style={styles.input}
        secureTextEntry
        onChangeText={setPassword}
      />

      {/* Password Strength */}
      <Text style={{ marginVertical: 5 }}>
        Password strength: {password.length > 5 ? 'Strong' : 'Weak'}
      </Text>

      {/* CUSTOM CHECKBOX */}
      <TouchableOpacity
        style={styles.checkboxContainer}
        onPress={() => setChecked(!checked)}
      >
        <View
          style={[
            styles.checkbox,
            { backgroundColor: checked ? '#6a11cb' : '#fff' },
          ]}
        />
        <Text>I agree with Privacy Policy</Text>
      </TouchableOpacity>

      {/* Button */}
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Create Account</Text>
      </TouchableOpacity>

    </View>
  );
};

export default CreateAccountScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: '#fff',
  },
  header: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 20,
  },
  socialBtn: {
    padding: 15,
    backgroundColor: '#eee',
    marginVertical: 5,
    borderRadius: 5,
  },
  separator: {
    marginVertical: 10,
    textAlign: 'center',
  },
  input: {
    borderWidth: 1,
    marginVertical: 8,
    padding: 10,
    borderRadius: 5,
  },
  checkboxContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 10,
  },
  checkbox: {
    width: 20,
    height: 20,
    borderWidth: 1,
    marginRight: 10,
  },
  button: {
    backgroundColor: '#6a11cb',
    padding: 15,
    borderRadius: 5,
  },
  buttonText: {
    color: '#fff',
    textAlign: 'center',
  },
});
